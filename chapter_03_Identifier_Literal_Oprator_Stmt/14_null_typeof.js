let selectedColor = null;
let selectItem = null;
let selectCar ;  // value not assigned, so it is undefined automatically

console.log(selectedColor); // null
console.log(selectItem); // null
console.log(selectCar); // undefined

// undefined --> not assigned a value
// null --> assigned a value but that value is null (empty)