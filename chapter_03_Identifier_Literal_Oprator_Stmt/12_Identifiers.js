// Identifiers are names used to identify variables, functions, and other entities in JavaScript. 
// They must follow certain rules:
// 1. An identifier must start with a letter, underscore (_), or dollar sign ($).
// 2. Subsequent characters can be letters, digits, underscores, or dollar signs.
// 3. Identifiers cannot be reserved keywords in JavaScript (e.g., var, function, if, etc.).
// 4. Identifiers are case-sensitive (e.g., myVariable and myvariable are different).

//Ex
// let return = "bj" ; // This will cause a syntax error because
                    //  "return" is a reserved keyword in JavaScript.


///////////////////////

// Literals
// Types of literals in JavaScript include like numeric literals, string literals, boolean literals,
//  object literals, array literals, null literals, and undefined literals.
let age = 25; // Numeric literal
let name = "Alice"; // string literal
let isStudent = true; // Boolean literal
let person = { name: "Bob", age: 30 }; // Object literal
let numbers = [1, 2, 3, 4, 5]; // Array literal
let nullValue = null; // Null literal
let undefinedValue; // Undefined literal (implicitly assigned)  
